diff --git a/site/src/contracts/coverage.ts b/site/src/contracts/coverage.ts
index ed24003..6af482f 100644
--- a/site/src/contracts/coverage.ts
+++ b/site/src/contracts/coverage.ts
@@ -32,7 +32,7 @@ export interface CoverageReport {
 
 const statuses = ["available", "partial", "missing", "not_applicable"] as const;
 const subjectTypes = ["method", "problem", "feature_family"] as const;
-const dimensions = ["map", "recommendation", "content", "visualization", "comparison", "gallery", "implementation", "journey", "source"] as const;
+const dimensions = ["map", "recommendation", "content", "visualization", "comparison", "gallery", "implementation", "failure", "journey", "source"] as const;
 
 export function parseCoverageReport(input: unknown): CoverageReport {
   const data = object(input, "coverage");
diff --git a/site/src/contracts/manifest.test.ts b/site/src/contracts/manifest.test.ts
index 2df88bb..12023a6 100644
--- a/site/src/contracts/manifest.test.ts
+++ b/site/src/contracts/manifest.test.ts
@@ -3,7 +3,7 @@ import { describe, expect, test } from "vitest";
 import { parseSiteManifest } from "./manifest";
 
 const manifest = {
-  version: "1.3.0",
+  version: "1.4.0",
   dataset_version: "0.2.0",
   generated_at: "2026-07-13T00:00:00Z",
   views: [{ view_id: "problem-structure", version: "1.0.0", path: "views/problem-structure.json" }],
@@ -18,6 +18,7 @@ const manifest = {
   implementation_claims: { version: "1.0.0", path: "implementation-claims.json" },
   benchmark_contexts: { version: "1.0.0", path: "benchmark-contexts.json" },
   failure_modes: { version: "1.0.0", path: "failure-modes.json" },
+  failure_discovery: { version: "1.0.0", path: "failure-discovery.json" },
   search_index: { version: "1.0.0", path: "search-index.json" },
   retrieval_documents: { version: "1.0.0", path: "retrieval-documents.json" },
   search_benchmark: { version: "1.0.0", path: "search-benchmark.json" },
@@ -49,6 +50,7 @@ describe("SiteManifest parser", () => {
     expect(parseSiteManifest(manifest).derived_media.path).toBe("media/manifest.json");
     expect(parseSiteManifest(manifest).benchmark_contexts.path).toBe("benchmark-contexts.json");
     expect(parseSiteManifest(manifest).failure_modes.path).toBe("failure-modes.json");
+    expect(parseSiteManifest(manifest).failure_discovery.path).toBe("failure-discovery.json");
     expect(parseSiteManifest(manifest).search_index.path).toBe("search-index.json");
     expect(parseSiteManifest(manifest).licenses.data.spdx_id).toBe("CC-BY-4.0");
   });
diff --git a/site/src/contracts/manifest.ts b/site/src/contracts/manifest.ts
index 19e6656..94743ee 100644
--- a/site/src/contracts/manifest.ts
+++ b/site/src/contracts/manifest.ts
@@ -44,7 +44,7 @@ export interface SiteLicenseManifest {
 }
 
 export interface SiteManifest {
-  version: "1.3.0";
+  version: "1.4.0";
   dataset_version: string;
   generated_at: string;
   views: ManifestView[];
@@ -60,6 +60,7 @@ export interface SiteManifest {
   implementation_claims: ManifestAsset;
   benchmark_contexts: ManifestAsset;
   failure_modes: ManifestAsset;
+  failure_discovery: ManifestAsset;
   search_index: ManifestAsset;
   retrieval_documents: ManifestAsset;
   search_benchmark: ManifestAsset;
@@ -88,6 +89,7 @@ export function parseSiteManifest(input: unknown): SiteManifest {
       "implementation_claims",
       "benchmark_contexts",
       "failure_modes",
+      "failure_discovery",
       "search_index",
       "retrieval_documents",
       "search_benchmark",
@@ -96,7 +98,7 @@ export function parseSiteManifest(input: unknown): SiteManifest {
     ],
     "SiteManifest",
   );
-  if (data.version !== "1.3.0") throw new Error("Unsupported SiteManifest version.");
+  if (data.version !== "1.4.0") throw new Error("Unsupported SiteManifest version.");
 
   const views = array(data.views, "views").map((value, index): ManifestView => {
     const view = record(value, `views[${index}]`);
@@ -159,6 +161,9 @@ export function parseSiteManifest(input: unknown): SiteManifest {
   const failureModes = record(data.failure_modes, "failure_modes");
   exactKeys(failureModes, ["version", "path"], "failure_modes");
   if (failureModes.version !== "1.0.0") throw new Error("failure_modes.version is unsupported.");
+  const failureDiscovery = record(data.failure_discovery, "failure_discovery");
+  exactKeys(failureDiscovery, ["version", "path"], "failure_discovery");
+  if (failureDiscovery.version !== "1.0.0") throw new Error("failure_discovery.version is unsupported.");
   const searchIndex = record(data.search_index, "search_index");
   exactKeys(searchIndex, ["version", "path"], "search_index");
   if (searchIndex.version !== "1.0.0") throw new Error("search_index.version is unsupported.");
@@ -186,7 +191,7 @@ export function parseSiteManifest(input: unknown): SiteManifest {
   }
 
   return {
-    version: "1.3.0",
+    version: "1.4.0",
     dataset_version: nonEmptyString(data.dataset_version, "dataset_version"),
     generated_at: nonEmptyString(data.generated_at, "generated_at"),
     views,
@@ -241,6 +246,10 @@ export function parseSiteManifest(input: unknown): SiteManifest {
       version: "1.0.0",
       path: safeRelativePath(failureModes.path, "failure_modes.path"),
     },
+    failure_discovery: {
+      version: "1.0.0",
+      path: safeRelativePath(failureDiscovery.path, "failure_discovery.path"),
+    },
     search_index: {
       version: "1.0.0",
       path: safeRelativePath(searchIndex.path, "search_index.path"),
diff --git a/site/src/contracts/search-index.ts b/site/src/contracts/search-index.ts
index 152b7fd..c36e1d9 100644
--- a/site/src/contracts/search-index.ts
+++ b/site/src/contracts/search-index.ts
@@ -1,11 +1,11 @@
 export const SEARCH_ENTITY_TYPES = [
   "case", "comparison", "content", "feature", "feature_value", "glossary",
-  "implementation", "journey", "method", "problem", "scenario", "source", "trace", "view",
+  "implementation", "journey", "method", "failure", "problem", "scenario", "source", "trace", "view",
 ] as const;
 
 export const SEARCH_INTENTS = [
   "classify_problem", "understand_method", "find_implementation",
-  "compare_visualize", "check_evidence", "find_case",
+  "compare_visualize", "check_evidence", "find_case", "diagnose_failure",
 ] as const;
 
 export const SEARCH_FIELDS = [
@@ -75,7 +75,7 @@ const FIELD_WEIGHTS: Record<SearchField, number> = {
 };
 
 const TYPE_PRIORITY = new Map<SearchEntityType, number>(
-  ["method", "problem", "case", "implementation", "journey", "content", "glossary", "comparison", "scenario", "trace", "source", "feature", "feature_value", "view"]
+  ["method", "problem", "case", "failure", "implementation", "journey", "content", "glossary", "comparison", "scenario", "trace", "source", "feature", "feature_value", "view"]
     .map((type, index) => [type as SearchEntityType, index]),
 );
 
diff --git a/site/src/features/coverage/CoveragePage.tsx b/site/src/features/coverage/CoveragePage.tsx
index e38b2e3..3ad50bc 100644
--- a/site/src/features/coverage/CoveragePage.tsx
+++ b/site/src/features/coverage/CoveragePage.tsx
@@ -55,7 +55,7 @@ function CoverageView({ report, journeys, subjectType, status, family, setSubjec
       <label>Subject<select value={subjectType} onChange={(event) => setSubjectType(event.target.value as SubjectType | "all")}><option value="all">すべて</option><option value="method">手法</option><option value="problem">問題型</option><option value="feature_family">特徴ファミリー</option></select></label>
       <label>Family<select value={family} onChange={(event) => setFamily(event.target.value)}><option value="all">すべて</option>{families.map((item) => <option key={item}>{item}</option>)}</select></label>
       <label>Status<select value={status} onChange={(event) => setStatus(event.target.value as CoverageStatus | "all")}><option value="all">すべて</option>{Object.entries(statusLabels).map(([key, label]) => <option value={key} key={key}>{label}</option>)}</select></label>
-    </div><div className="coverage-table-wrap" role="region" aria-label="Artifact inventory table" tabIndex={0}><table><thead><tr><th>Subject</th><th>Type</th><th>接続</th><th>期待状態</th></tr></thead><tbody>{visible.map((subject) => { const expectations = expectationBySubject.get(subject.subject_id) ?? []; const connected = Object.values(subject.dimensions).filter((item) => item.state === "connected").length; return <tr key={`${subject.subject_type}:${subject.subject_id}`}><th scope="row"><span>{subject.label}</span><code>{subject.subject_id}</code></th><td>{subject.subject_type}</td><td>{connected}/8</td><td>{expectations.length ? expectations.map((item) => <details key={item.expectation_id}><summary><span className={`coverage-pill coverage-${item.status}`}>{statusLabels[item.status]}</span> {item.purpose}</summary><p>{item.rationale}</p>{item.reason_codes.map((reason) => <code key={reason}>{reason}</code>)}</details>) : <span className="muted">期待値なし</span>}</td></tr>; })}</tbody></table></div></section>
+    </div><div className="coverage-table-wrap" role="region" aria-label="Artifact inventory table" tabIndex={0}><table><thead><tr><th>Subject</th><th>Type</th><th>接続</th><th>期待状態</th></tr></thead><tbody>{visible.map((subject) => { const expectations = expectationBySubject.get(subject.subject_id) ?? []; const connected = Object.values(subject.dimensions).filter((item) => item.state === "connected").length; return <tr key={`${subject.subject_type}:${subject.subject_id}`}><th scope="row"><span>{subject.label}</span><code>{subject.subject_id}</code></th><td>{subject.subject_type}</td><td>{connected}/{Object.keys(subject.dimensions).length}</td><td>{expectations.length ? expectations.map((item) => <details key={item.expectation_id}><summary><span className={`coverage-pill coverage-${item.status}`}>{statusLabels[item.status]}</span> {item.purpose}</summary><p>{item.rationale}</p>{item.reason_codes.map((reason) => <code key={reason}>{reason}</code>)}</details>) : <span className="muted">期待値なし</span>}</td></tr>; })}</tbody></table></div></section>
     {report.integrity_issues.length > 0 && <section aria-labelledby="integrity-title"><h2 id="integrity-title">Broken references</h2><ul className="coverage-issues">{report.integrity_issues.map((item) => <li key={`${item.code}:${item.entity_id}`}><code>{item.code}</code> <strong>{item.entity_id}</strong> — {item.detail}</li>)}</ul></section>}
   </section>;
 }
diff --git a/site/src/features/failures/FailureModePage.test.tsx b/site/src/features/failures/FailureModePage.test.tsx
index 4a9e28f..3650f4c 100644
--- a/site/src/features/failures/FailureModePage.test.tsx
+++ b/site/src/features/failures/FailureModePage.test.tsx
@@ -2,15 +2,16 @@ import { cleanup, fireEvent, render, screen, within } from "@testing-library/rea
 import { MemoryRouter } from "react-router-dom";
 import { afterEach, describe, expect, test, vi } from "vitest";
 
-import rawFailureModes from "../../../public/data/failure-modes.json";
+import rawFailureDiscovery from "../../../public/data/failure-discovery.json";
+import rawManifest from "../../../public/data/manifest.json";
 import type { EntityLinkIndex } from "../../contracts/entity-links";
 import { EntityLinkProvider } from "../../state/entity-links";
 import { FailureModePage } from "./FailureModePage";
 
 const emptyLinks: EntityLinkIndex = {
   contract_version: "1.0.0",
-  dataset_version: "0.11.0",
-  generated_at: "2026-07-16T00:00:00Z",
+  dataset_version: rawFailureDiscovery.dataset_version,
+  generated_at: "2026-07-17T00:00:00Z",
   entities: [],
 };
 
@@ -19,20 +20,30 @@ afterEach(() => {
   vi.unstubAllGlobals();
 });
 
-describe("FailureModePage", () => {
-  test("starts from symptoms and keeps detailed relations collapsed", async () => {
-    vi.stubGlobal("fetch", vi.fn().mockResolvedValue({
+function mockDiscovery(): void {
+  vi.stubGlobal("fetch", vi.fn().mockImplementation(async (input: string | URL | Request) => {
+    const url = String(input);
+    return {
       ok: true,
-      json: async () => rawFailureModes,
-    }));
+      json: async () => url.endsWith("manifest.json") ? rawManifest : rawFailureDiscovery,
+    };
+  }));
+}
+
+function renderPage(entry = "/failures") {
+  return render(
+    <MemoryRouter initialEntries={[entry]}>
+      <EntityLinkProvider initialIndex={emptyLinks}>
+        <FailureModePage />
+      </EntityLinkProvider>
+    </MemoryRouter>,
+  );
+}
 
-    render(
-      <MemoryRouter>
-        <EntityLinkProvider initialIndex={emptyLinks}>
-          <FailureModePage />
-        </EntityLinkProvider>
-      </MemoryRouter>,
-    );
+describe("FailureModePage", () => {
+  test("starts from symptoms and keeps detailed relations collapsed", async () => {
+    mockDiscovery();
+    renderPage();
 
     const card = (await screen.findByRole("heading", {
       level: 2,
@@ -42,29 +53,44 @@ describe("FailureModePage", () => {
     expect(within(card!).getByText("gradient符号がreplicateで変わる")).toBeVisible();
     expect(within(card!).getByText("replication/CRN")).toBeVisible();
     expect(within(card!).getByText("sample average、SPSA、noise-aware method")).toBeVisible();
-    expect(within(card!).getByText("影響する手法・可視化・根拠")).not.toHaveAttribute("open");
+    expect(within(card!).getByText("文脈・関連するrun・根拠").closest("details"))
+      .not.toHaveAttribute("open");
   });
 
-  test("filters the canonical failure list by symptom or title", async () => {
-    vi.stubGlobal("fetch", vi.fn().mockResolvedValue({
-      ok: true,
-      json: async () => rawFailureModes,
-    }));
-
-    render(
-      <MemoryRouter>
-        <EntityLinkProvider initialIndex={emptyLinks}>
-          <FailureModePage />
-        </EntityLinkProvider>
-      </MemoryRouter>,
-    );
+  test("shows a Case-specific exclusion without presenting it as a universal failure", async () => {
+    mockDiscovery();
+    renderPage();
 
     await screen.findByRole("heading", { level: 2, name: "noiseが微分を支配" });
-    fireEvent.change(screen.getByRole("searchbox", { name: "失敗の兆候を検索" }), {
+    fireEvent.change(screen.getByLabelText("種類"), { target: { value: "case_exclusion" } });
+
+    const card = screen.getByRole("heading", {
+      level: 2,
+      name: "強度制約を守りながら軽量設計を探すではBFGS法を選ばない",
+    }).closest("article");
+    expect(card).not.toBeNull();
+    expect(within(card!).getByText("Case固有")).toBeVisible();
+    expect(within(card!).getByText(/非線形constraintを無視/u)).toBeVisible();
+    expect(within(card!).queryByText("high")).not.toBeInTheDocument();
+  });
+
+  test("filters by symptom/reason and opens a directly addressed discovery entry", async () => {
+    mockDiscovery();
+    renderPage("/failures?entry=case%3AEC025%3AM_DIFFERENTIAL_EVOLUTION");
+
+    const focused = (await screen.findByRole("heading", {
+      level: 2,
+      name: /100Hzで反復MPC/u,
+    })).closest("article");
+    expect(focused).toHaveClass("is-focused");
+    expect(within(focused!).getByText("文脈・関連するrun・根拠").closest("details"))
+      .toHaveAttribute("open");
+
+    fireEvent.change(screen.getByRole("searchbox", { name: "失敗・除外を検索" }), {
       target: { value: "悪条件" },
     });
-
     expect(screen.getByRole("heading", { level: 2, name: "悪条件" })).toBeVisible();
-    expect(screen.queryByRole("heading", { level: 2, name: "noiseが微分を支配" })).not.toBeInTheDocument();
+    expect(screen.queryByRole("heading", { level: 2, name: /100Hzで反復MPC/u }))
+      .not.toBeInTheDocument();
   });
 });
