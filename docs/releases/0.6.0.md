# Dataset release 0.6.0

Released 2026-07-15.

This release publishes four preset-driven semantic Views with stable public
IDs, explicit purpose and limitations, authored canonical filters, and a
deterministic focus fallback policy.

The generated artifacts remain `ViewSpec 1.0.0`; the contract now carries the
preset metadata needed for View selection without introducing a parallel
schema or handwritten tree JSON.
