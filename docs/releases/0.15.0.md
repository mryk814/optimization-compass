# Dataset 0.15.0

- Completed Gallery case `EC017` as the fifth canonical Case → Theater → Compare learning journey.
- Distinguished the real cost-performance Case from the fixed educational problem
  `INSTANCE_BIOBJECTIVE_QUADRATIC_2D`, including its box bounds and two explicit objectives.
- Added derived sensitivity scenario `SCENARIO_BIOBJECTIVE_PREFERENCE_SENSITIVITY`, reusing the
  canonical Pareto artifact while separating front generation from preference-based point selection.
- Revised `COMPARE_PARETO_PREFERENCE` to compare weighted-sum selections at
  `weight_f1 = 0.2`, `0.5`, and `0.8` over the same 81 sampled points without ranking methods.
- Added fail-closed comparison checks that bind each member weight to the canonical selected decision
  and objective values instead of recomputing or inventing missing results in the browser.
- Exposed sampled, dominated, non-dominated, and analytic-reference counts without presenting the
  sampled or analytic sets as an algorithmic coverage guarantee.
- Advanced the learning-journey milestone to five complete and six partial journeys.
- Prepared complete distributions under the external release-bundle retention contract. Remote
  publication and atomic tracked promotion remain separate release operations.
