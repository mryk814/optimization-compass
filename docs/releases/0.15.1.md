# Dataset 0.15.1

- Corrected the method/profile identity of the derived preference-sensitivity scenario.
- Kept `SCENARIO_BIOBJECTIVE_PREFERENCE_SENSITIVITY` bound to the same canonical
  `M_NSGA_II` front-generation run and artifact as its primary scenario.
- Kept `M_WEIGHTED_SUM` on the three `COMPARE_PARETO_PREFERENCE` members, where the
  weights select already-recorded canonical points instead of claiming to generate the front.
- Added a regression assertion that the primary and derived scenarios share an identical run.
- Retained dataset 0.15.0 unchanged and superseded it with this corrective release.
