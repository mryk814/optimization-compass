# Dataset 0.10.0

- Added canonical `feasible_region` and `pareto_front` renderer-family learning slices.
- Added deterministic constrained-continuous failure contrast and bi-objective result artifacts.
- Connected Content, Gallery, Map, Method pages, Theater routes, and sources through canonical IDs.
- Corrected the constrained disk instance so its inequality and known optimum use the same feasible set.
- Marked both priority slices as available in generated Coverage data.
