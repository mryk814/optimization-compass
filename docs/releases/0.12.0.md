# Dataset 0.12.0

- Added the canonical `PROBLEM_NONLINEAR_LEAST_SQUARES` definition and
  `INSTANCE_EXPONENTIAL_DECAY_FIT_3P` executable instance for a fixed 20-point,
  three-parameter exponential-decay lesson.
- Connected Gallery case `EC013` to primary TRF, poor-initialization sensitivity,
  conditional LM, and scalar-objective L-BFGS-B teaching paths.
- Added `COMPARE_EXPONENTIAL_FIT_SOLVER_CONDITIONS` with a fixed model, data,
  normal initial point, bounds, and 12-oracle-evaluation context.
- Published residual norm, gradient norm, Jacobian rank, and parameter-error
  histories with the existing `AlgorithmTrace` 1.0 and `generic_metric_history`
  renderer contracts.
- Marked every path as educational and implementation mapping as not applicable;
  none of the traces represents SciPy or Ceres internal iterations or a general
  solver ranking.
- Published the deterministic database, runtime resource, site, search, retrieval,
  and distribution artifacts from the same validated release pipeline.

