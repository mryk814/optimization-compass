# Dataset 0.11.0

- Added `M_TRUST_REGION_REFLECTIVE` as a canonical bound-constrained nonlinear least-squares method.
- Reconnected SciPy `least_squares(method='trf')` from broad Gauss–Newton / Krylov labels to the dedicated method.
- Recorded the SciPy `least_squares` default and the bounds-dependent `curve_fit` default through the versioned implementation default claim.
- Added the primary Branch–Coleman–Li reference and a beginner-first method guide.
- Started a library-default audit that distinguishes API behavior from Optimization Compass recommendation priority.
- Published the deterministic database, runtime resource, site, search, retrieval, and distribution artifacts from the same validated release pipeline.
