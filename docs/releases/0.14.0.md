# Dataset 0.14.0

- Corrected Gallery case `budget-allocation` from continuous unconstrained archetype `PA006` to
  binary combinatorial archetype `PA032`, and aligned its formulation with
  `INSTANCE_BINARY_KNAPSACK_4`.
- Replaced the mismatched linear-solver snippet with an OR-Tools CP-SAT 0-1 knapsack example and
  added complete Diagnose answers, direct evidence, implementation, method-guide, and limitation
  links.
- Reused the existing complete and budget-stopped Branch-and-Bound search-tree scenarios with a
  common nine-evaluation allowance and member stop limits of nine and four explored nodes.
- Updated the deterministic search-tree generator to 1.1.0 so the common evaluation allowance and
  the member-specific stopping rule are exported separately and truthfully.
- Added canonical comparison `COMPARE_KNAPSACK_BNB_BUDGET`, a non-ranking one-factor failure
  contrast of incumbent, global bound, gap, feasibility, and stopping status.
- Added exact context `BENCH_KNAPSACK_BNB_EDUCATIONAL_9` with seed 0, fixed generator identity,
  explicit `implementation_mapping_status=not_applicable`, and cross-validation against both
  scenario runs.
- Prepared complete distributions under the external release-bundle retention contract. Remote
  publication and atomic tracked promotion remain separate release operations.
