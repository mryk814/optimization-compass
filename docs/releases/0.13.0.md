# Dataset 0.13.0

- Corrected the expensive black-box/HPO Gallery case to canonical archetype `PA039` and added
  complete formulation, diagnostic answers, direct sources, implementations, and limitations.
- Connected the case to `OBJECTIVE_EDUCATIONAL_WAVY_1D` and four existing
  `SurrogateUncertainty` 1.0 scenarios without introducing a new renderer or artifact contract.
- Added `COMPARE_BO_ACQUISITION_NOISE_BASELINE`, a non-ranking 10-evaluation contrast of
  acquisition exploration, small observation noise, and a uniform-random baseline.
- Added exact benchmark context `BENCH_BO_EDUCATIONAL_10` with seed `2604`, fixed initial
  design, deterministic educational-generator identity, and implementation mapping explicitly
  marked not applicable.
- Cross-validated the comparison, benchmark context, scenario experiments, and member runs so
  a mismatched problem, budget, seed, initial design, generator, or implementation mapping fails
  release generation.
- Replaced query-based Bayesian Optimization scenario selection with canonical scenario paths;
  unknown scenario IDs now return Not Found and Case journey state survives preset changes.
- Reused the shared surrogate plot in Compare Lab, preserved the first routable Theater member
  when multiple members share one scenario, and retained an end-to-end route back to the Case.
- Prepared complete distributions under the external release-bundle retention contract. Remote
  publication and atomic tracked promotion remain separate release operations.
