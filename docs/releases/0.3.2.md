# Dataset release 0.3.2

Release date: 2026-07-15

## Fixed

- Reconciled canonical scenario IDs with generated traces and declared derived/generated-only
  scenario status instead of reporting ambiguous orphan warnings.
- Aligned the canonical first-order comparison with the published quadratic comparison artifact.
- Added an alias for the former `COMPARE_FIRST_ORDER_ROSENBROCK` deep link.
- Made unresolved canonical scenario or comparison references release-blocking coverage errors.

## Compatibility

The public comparison route `#/compare/COMPARE_FIRST_ORDER_ROSENBROCK` remains supported as an
alias for `COMPARE_GRADIENT_FAMILY`. Consumers must accept dataset version `0.3.2` and replace all
generated data artifacts atomically.
